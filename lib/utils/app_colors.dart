import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Logo-inspired medical red palette
  static const Color primary = Color(0xFFFF1F1F); // Main logo red
  static const Color primaryDark = Color(0xFFC81010); // Deep red
  static const Color primaryLight = Color(0xFFFFE5E5); // Soft red background

  // Secondary colors
  static const Color secondary = Color(0xFF2B2B2B); // Dark text from logo
  static const Color secondaryLight = Color(0xFFF4F4F4);

  // Accent colors
  static const Color accent = Color(0xFFFF4D4D); // Bright alert red
  static const Color warning = Color(0xFFFF9800);
  static const Color success = Color(0xFF22C55E);

  // Backgrounds
  static const Color background = Color(0xFFF8F8F8); // Clean white-gray
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceTint = Color(0xFFFFF5F5);

  // Text colors
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF666666);

  // Borders & shadows
  static const Color border = Color(0xFFE5E5E5);
  static const Color shadow = Color(0x1AFF1F1F);
}
